import Title from './Title';

export default function Header() {
    return (
        <header className="fixed flex w-full items-center justify-center py-4">
            <Title />
        </header>
    );
}
